// Canonical list of LIVE pages, used to build the XML sitemap.
// Add a page's path here when you build it so the sitemap stays accurate.
// (Only include pages that actually exist — never list 404s in a sitemap.)
export const liveRoutes = [
  '/',
  '/services/',
  '/portfolio/',
  '/about/',
  '/careers/',
  '/blog/',
  '/faq/',
  '/contact/',
  // Individual service detail pages get added here as they are built:
  // '/custom-landscape-design/', '/paver-patios/', etc.
];
